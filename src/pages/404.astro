---
import Layout from '~/layouts/Layout.astro';
import { getHomePermalink } from '~/utils/permalinks';

const title = `Error 404`;
---

<Layout metadata={{ title }}>
  <section class="flex items-center min-h-[calc(100vh-100px)] p-16 relative overflow-hidden">
    <!-- 背景饰点 -->
    <div class="absolute inset-0 pointer-events-none">
      <div class="absolute top-1/4 left-1/4 w-96 h-96 bg-blue-500/10 rounded-full blur-[120px]"></div>
      <div class="absolute bottom-1/4 right-1/4 w-96 h-96 bg-indigo-500/10 rounded-full blur-[120px]"></div>
    </div>

    <div class="container flex flex-col items-center justify-center px-5 mx-auto relative z-10">
      <div
        class="max-w-2xl w-full bg-white/40 dark:bg-slate-900/40 backdrop-blur-xl border border-white/50 dark:border-slate-800/50 rounded-[3rem] p-12 md:p-20 shadow-[0_1px_2px_rgba(0,0,0,0.05),0_20px_40px_-10px_rgba(0,0,0,0.05)] text-center"
      >
        <h2 class="mb-8 font-black text-8xl md:text-9xl tracking-tighter">
          <span class="sr-only">Error</span>
          <span class="bg-clip-text text-transparent bg-gradient-to-br from-blue-600 to-indigo-500">404</span>
        </h2>
        <p class="text-2xl md:text-3xl font-black text-slate-800 dark:text-white mb-6">页面似乎在异次元迷路了</p>
        <p class="mt-4 mb-12 text-lg text-slate-500 dark:text-slate-400 font-medium leading-relaxed">
          抱歉，我们找不到您请求的页面。可能是链接已失效，或者它正在进行深度维护。
        </p>

        <div class="flex flex-col sm:flex-row gap-5 justify-center">
          <a
            href={getHomePermalink()}
            class="group relative inline-flex items-center justify-center p-0.5 overflow-hidden text-sm font-bold text-white rounded-full group bg-gradient-to-br from-blue-600 to-indigo-500 hover:text-white dark:text-white focus:ring-4 focus:outline-none focus:ring-blue-300 dark:focus:ring-blue-800 shadow-[0_1px_15px_rgba(37,99,235,0.4)] hover:shadow-[0_1px_25px_rgba(37,99,235,0.6)] transition-all duration-300 active:scale-95"
          >
            <span
              class="relative px-12 py-4 transition-all ease-in duration-75 bg-slate-900/10 rounded-full group-hover:bg-opacity-0 text-lg"
            >
              返回首页
            </span>
          </a>
        </div>
      </div>
    </div>
  </section>
</Layout>
